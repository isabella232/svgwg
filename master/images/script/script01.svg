<svg xmlns="http://www.w3.org/2000/svg" width="6cm" height="5cm" version="1.1" viewBox="0 0 600 500"><desc>Example script01 - invoke an ECMAScript function from an onclick event</desc><script type="application/ecmascript"><![CDATA[
    function circle_click(evt) {
      var circle = evt.target;
      var currentRadius = circle.getAttribute("r");
      if (currentRadius == 100)
        circle.setAttribute("r", currentRadius*2);
      else
        circle.setAttribute("r", currentRadius*0.5);
    }
  ]]></script><rect width="598" height="498" x="1" y="1" fill="none" stroke="#00f"/><circle cx="300" cy="225" r="100" fill="red" onclick="circle_click(evt)"/><text x="300" y="480" font-family="Verdana" font-size="35" text-anchor="middle">Click on circle to change its size</text></svg>